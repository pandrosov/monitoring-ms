# Telegram-бот мониторинга

## Доступ

- Отправьте ответственному за доступ (Павел Андросов, Telegram user ID `pandrosov`) свой Telegram ID, ФИО и рабочий e-mail.
- Узнать свой ID можно через бота `@userinfobot`.
- После добавления ID в список разрешённых бот станет доступен; без разрешения команды игнорируются.

## Ограничения

- Для каждого региона может выполняться только одна проверка одновременно. Если по РБ уже идёт проверка, повторный запуск по РБ будет заблокирован до завершения.
- Во время активной проверки бот предложит дождаться окончания или выбрать другой регион/тип документа.
- После завершения бот уведомит о готовности и разблокирует регион.

## Пошаговое использование

1. Откройте чат с ботом и отправьте `/start`.
2. Выберите регион: `🇧🇾 РБ`, `🇷🇺 РФ` или `🇰🇿 КЗ`. Выбор сохраняется до следующей смены.
3. Выберите тип документа (`📦 Отгрузки`, `💰 Продажи`, `📋 Комиссионеры`, `👥 Контрагенты`) — бот подскажет, что будет проверено.
4. Выберите период: готовые опции (`Сегодня`, `Вчера`, `Неделя`, `Месяц`, `Последние 3 дня`) или ручной ввод дат.
5. Укажите, нужно ли автоматически отправить результат в Bitrix24.
6. Дождитесь окончания проверки. Бот покажет статистику, список ошибок и предложит:
   - `🔄 Новая проверка` — вернуться к шагу выбора региона;
   - `📊 В Bitrix24` / `📊 Отправить повторно` — отправить сводку в чат.

## Быстрые команды

- `/start` — новая проверка.
- `/help` — справка по возможностям.
- `/cancel` — отмена текущего сценария.

## Работа в фоне

Запуск через Docker:

```bash
make bot DETACH=1
```

Ручной запуск:

```bash
python telegram_bot.py
```

При необходимости следите за файлами логов (директория `logs/`, если она не исключена) или используйте консольный вывод.

